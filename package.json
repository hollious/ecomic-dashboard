{
  "name": "test",
  "version": "1.0.0",
  "description": "기본 웹 프로젝트 템플릿입니다.",
  "main": "config.js",
  "scripts": {
    "proxy": "node proxy-server.js",
    "web": "python3 -m http.server 8000",
    "dev": "concurrently \"npm run proxy\" \"npm run web\"",
    "start": "node proxy-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
